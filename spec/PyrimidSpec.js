/*
var assert = require('assert')
import Simple from "../lib/Simple"

describe('Simple', () => {
  var klass

  beforeEach(() => {
    klass = new Simple
  })

  it('works 1', () => {
    assert(true)
  })

  it('works 2', () => {
    assert(klass.two() === 2)
  })

  it('works 3', () => {
    assert(Simple.three() === 9)
  })

})
*/
